pyspypoint

# Cloudflare R2 image hosting (S3-compatible). Needed only when the R2_* env vars
# are set to host photos outside the repo; harmless to have installed otherwise.
boto3

# Image handling: Pillow downscales photos for faster loads AND lets us OCR the
# temperature SPYPOINT stamps onto each photo. pytesseract drives the OCR and also
# needs the `tesseract` binary on PATH (CI installs it; locally: winget install tesseract).
Pillow
pytesseract

