// TrailHub configuration (safe to commit; contains no secrets).
//
// SHARED TAGS — make tag edits visible to everyone, on every machine.
// Leave this blank and tag edits stay only in the browser that made them.
// Set it to your tag endpoint URL to sync tags for all visitors.
// See the README section "Shared tags (so everyone sees them)" for the
// one-time setup that gives you this URL.
window.TRAILHUB_TAGS_API = "";
